import { rules } from "../src/rules.js";
import { expect } from "chai";

describe("hooks", function () {
  beforeEach(function () {
    myAttr = {
      str: 1,
      dex: 1,
      foc: 1,
      vit: 1,
    };
  });

  describe("Testing duration multiplier");
});
